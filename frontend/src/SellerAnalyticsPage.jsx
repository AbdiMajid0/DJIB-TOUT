import React from 'react'
import {BarChart3,Download,Package,Receipt,RotateCcw,ShoppingBag,TrendingUp} from 'lucide-react'
import {api} from './lib/api'
import './seller-analytics.css'
const money=n=>new Intl.NumberFormat('fr-FR').format(Number(n||0))+' FDJ'
export default function SellerAnalyticsPage(){const [days,setDays]=React.useState(30),[data,setData]=React.useState(null),[error,setError]=React.useState('');React.useEffect(()=>{setData(null);api(`/seller/analytics?days=${days}`).then(setData).catch(e=>setError(e.message))},[days]);async function download(){const token=localStorage.getItem('dt.accessToken'),base=import.meta.env.VITE_API_URL||'/api',response=await fetch(`${base}/seller/analytics/export?days=${days}`,{headers:{Authorization:`Bearer ${token}`}});if(!response.ok)return setError('Export impossible.');const url=URL.createObjectURL(await response.blob()),a=document.createElement('a');a.href=url;a.download=`statistiques-${days}-jours.csv`;a.click();URL.revokeObjectURL(url)}if(!data)return <div className="portal-content analytics-loading">Chargement des statistiques…</div>;const max=Math.max(1,...data.topProducts.map(x=>Number(x.revenue)));return <div className="portal-content seller-analytics"><header><div><small>PERFORMANCE</small><h1>Statistiques</h1><p>Analysez les ventes réelles de votre boutique.</p></div><div><select value={days} onChange={e=>setDays(Number(e.target.value))}><option value="7">7 derniers jours</option><option value="30">30 derniers jours</option><option value="90">90 derniers jours</option><option value="365">12 derniers mois</option></select><button onClick={download}><Download/> Exporter</button></div></header>{error&&<div className="api-error">{error}</div>}<div className="analytics-kpis"><article><i><TrendingUp/></i><span><small>CHIFFRE D’AFFAIRES</small><b>{money(data.revenue)}</b><em>Sur {days} jours</em></span></article><article><i><ShoppingBag/></i><span><small>COMMANDES</small><b>{data.orders}</b><em>Commandes valides</em></span></article><article><i><Receipt/></i><span><small>PANIER MOYEN</small><b>{money(data.averageOrder)}</b><em>Par commande</em></span></article><article><i><RotateCcw/></i><span><small>TAUX DE RETOUR</small><b>{data.returnRate}%</b><em>{data.returns} retour(s)</em></span></article></div><div className="analytics-layout"><section className="analytics-chart"><header><div><h2>Meilleurs produits</h2><p>Classement selon le chiffre d’affaires.</p></div><BarChart3/></header>{data.topProducts.length?<div className="analytics-bars">{data.topProducts.map((p,i)=><article key={p.name}><b>{i+1}</b><span><label><strong>{p.name}</strong><small>{p.units} unité(s)</small></label><i><em style={{width:`${Number(p.revenue)/max*100}%`}}/></i></span><strong>{money(p.revenue)}</strong></article>)}</div>:<div className="analytics-empty"><Package/><h3>Aucune vente sur cette période</h3><p>Essayez une période plus longue ou enrichissez votre catalogue.</p></div>}</section><aside className="analytics-summary"><h2>Résumé de la période</h2><div><span>Ventes brutes</span><b>{money(data.revenue)}</b></div><div><span>Commission estimée (10%)</span><b>− {money(Number(data.revenue)*.1)}</b></div><div className="net"><span>Net vendeur estimé</span><b>{money(Number(data.revenue)*.9)}</b></div><p>Les montants définitifs apparaissent dans « Règlements » après livraison des commandes.</p></aside></div></div>}
