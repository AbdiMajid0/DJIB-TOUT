"use client";
import { motion } from "framer-motion";
export default function StockMeter({stock}:{stock:number}){if(stock<=0)return <p className="text-sm font-bold text-red-600">Rupture de stock</p>;const reference=Math.max(stock,20);const percent=Math.max(5,Math.min(100,stock/reference*100));const tone=stock<=3?"bg-red-600":stock<=10?"bg-orange-500":"bg-emerald-500";return <div className="min-w-[150px] flex-1"><div className="flex justify-between text-xs"><strong className={stock<=3?"text-red-600":stock<=10?"text-orange-600":"text-emerald-700"}>{stock<=10?`Plus que ${stock} en stock`:`${stock} en stock`}</strong></div><div className="mt-1.5 h-1.5 overflow-hidden rounded-full bg-slate-200"><motion.div className={`h-full rounded-full ${tone}`} initial={{width:0}} animate={{width:`${percent}%`}} transition={{duration:.7,ease:"easeOut"}}/></div></div>}
