"use client";
import {useEffect,useMemo,useState} from "react";import {apiRequest} from "@/lib/api";
type User={id:number;name:string;email:string;role:string;emailVerified:boolean;suspended?:boolean};
export default function UsersPage(){const[users,setUsers]=useState<User[]>([]),[query,setQuery]=useState(""),[error,setError]=useState("");const load=()=>apiRequest<User[]>("/admin/users").then(setUsers).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);const rows=useMemo(()=>users.filter(u=>(u.name+u.email+u.role).toLowerCase().includes(query.toLowerCase())),[users,query]);const toggle=async(u:User)=>{try{await apiRequest(`/admin/users/${u.id}/suspension`,{method:"PATCH",body:JSON.stringify({suspended:!u.suspended})});load()}catch(e){setError(e instanceof Error?e.message:"Action impossible")}};return <section><h1 className="text-2xl font-black">Utilisateurs</h1><p className="mt-1 text-sm text-slate-500">Recherche et contrôle des comptes.</p><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Rechercher…" className="my-5 w-full max-w-md rounded-xl border px-4 py-3"/>{error&&<p className="mb-3 text-sm text-red-600">{error}</p>}<div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Nom</th><th>Email</th><th>Rôle</th><th>État</th><th></th></tr></thead><tbody>{rows.map(u=><tr key={u.id} className="border-t"><td className="p-4 font-bold">{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.suspended?<span className="text-red-600">Suspendu</span>:<span className="text-emerald-600">Actif</span>}</td><td><button disabled={u.role==="ADMIN"} onClick={()=>toggle(u)} className="rounded-lg border px-3 py-2 font-bold disabled:opacity-40">{u.suspended?"Réactiver":"Suspendre"}</button></td></tr>)}</tbody></table></div></section>}
