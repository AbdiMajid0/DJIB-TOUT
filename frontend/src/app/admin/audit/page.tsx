"use client";import {useEffect,useState} from "react";import {apiRequest} from "@/lib/api";type Log={id:number;action:string;target:string;createdAt:string;actor?:{email:string}};export default function Page(){const[logs,setLogs]=useState<Log[]>([]);useEffect(()=>{apiRequest<Log[]>("/admin/audit-logs").then(x=>setLogs(x.reverse()))},[]);return <div><h1 className="text-2xl font-black">Journal d’audit</h1><div className="mt-4 overflow-x-auto rounded-xl border bg-white"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr><th className="p-3">Date</th><th>Acteur</th><th>Action</th><th>Cible</th></tr></thead><tbody>{logs.map(x=><tr key={x.id} className="border-t"><td className="p-3">{new Date(x.createdAt).toLocaleString("fr-FR")}</td><td>{x.actor?.email||"—"}</td><td className="font-bold">{x.action}</td><td>{x.target}</td></tr>)}</tbody></table></div></div>}
