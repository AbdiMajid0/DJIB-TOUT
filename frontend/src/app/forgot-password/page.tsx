"use client";import {useState} from "react";import Link from "next/link";import {API_BASE_URL} from "@/lib/api";
export default function ForgotPasswordPage(){const [email,setEmail]=useState(""),[message,setMessage]=useState(""),[token,setToken]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const response=await fetch(`${API_BASE_URL}/auth/forgot-password`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const data=await response.json();setMessage(data.message);if(data.developmentResetToken)setToken(data.developmentResetToken)}return <main className="grid min-h-[70vh] place-items-center bg-[#f6f7f9] p-4"><section className="w-full max-w-md rounded-2xl border bg-white p-7"><Link href="/" className="text-2xl font-black">djib<span className="text-[var(--primary)]">tout</span></Link><h1 className="mt-7 text-2xl font-black">Mot de passe oublié</h1><p className="mb-6 mt-2 text-sm text-slate-500">Saisissez l’adresse associée à votre compte.</p><form onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="h-11 w-full rounded-lg border px-3" placeholder="Adresse email"/><button className="mt-5 w-full rounded-lg bg-[var(--primary)] py-3 text-sm font-black text-white">Continuer</button></form>{message&&<p className="mt-4 rounded-lg bg-blue-50 p-3 text-xs text-[var(--primary)]">{message}</p>}{token&&<Link href={`/reset-password?token=${token}`} className="mt-3 block text-center text-xs font-bold text-[var(--primary)]">Ouvrir le lien de développement</Link>}</section></main>}
