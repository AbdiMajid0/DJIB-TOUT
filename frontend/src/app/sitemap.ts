import type {MetadataRoute} from "next";import {fetchCatalogMetadata,fetchProducts} from "@/lib/api";import {productPath,SITE_URL,slugify} from "@/lib/seo";
export const revalidate=3600;
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const now=new Date();const base:MetadataRoute.Sitemap=[{url:SITE_URL,lastModified:now,changeFrequency:"daily",priority:1},{url:`${SITE_URL}/search`,lastModified:now,changeFrequency:"daily",priority:.8}];const[products,metadata]=await Promise.all([fetchProducts(),fetchCatalogMetadata()]);return [...base,...metadata.categories.map(c=>({url:`${SITE_URL}/category/${slugify(c.value)}`,lastModified:now,changeFrequency:"daily" as const,priority:.7})),...products.map(p=>({url:`${SITE_URL}${productPath(p)}`,lastModified:now,changeFrequency:"weekly" as const,priority:.8}))];}
