import type {Metadata} from "next";import {fetchProductById,fetchProducts} from "@/lib/api";import {notFound,permanentRedirect} from "next/navigation";import ProductDetailClient from "@/components/ProductDetailClient";import {productIdFromSegment,productPath,SITE_URL} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const{id}=await params;const productId=productIdFromSegment(id);const product=productId?await fetchProductById(productId):null;if(!product)return {title:"Produit introuvable",robots:{index:false,follow:false}};const description=(product.description||`Achetez ${product.name} sur DjibTout`).slice(0,155);const image=product.images?.find(value=>/^(https?:|\/)/.test(value));const path=productPath(product);return {title:product.name,description,alternates:{canonical:path},openGraph:{type:"website",url:path,title:product.name,description,images:image?[{url:image,alt:product.name}]:[]},twitter:{card:"summary_large_image",title:product.name,description,images:image?[image]:[]}};}
export default async function ProductPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const productId=productIdFromSegment(id);if(!productId)notFound();const[product,allProducts]=await Promise.all([fetchProductById(productId),fetchProducts()]);if(!product)notFound();const canonical=productPath(product);if(`/product/${id}`!==canonical)permanentRedirect(canonical);const images=(product.images||[]).filter(value=>/^(https?:|\/)/.test(value));const url=`${SITE_URL}${canonical}`;const schema={"@context":"https://schema.org","@type":"Product",name:product.name,description:product.description,image:images,sku:`DJT-${String(product.id).padStart(6,"0")}`,brand:product.brand?{"@type":"Brand",name:product.brand}:undefined,offers:{"@type":"Offer",priceCurrency:"DJF",price:product.price,availability:product.stockQuantity>0?"https://schema.org/InStock":"https://schema.org/OutOfStock",url,seller:product.seller?.name?{"@type":"Organization",name:product.seller.name}:undefined},aggregateRating:(product.reviewCount||0)>0?{"@type":"AggregateRating",ratingValue:product.averageRating,reviewCount:product.reviewCount}:undefined};const breadcrumb={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Accueil",item:SITE_URL},{"@type":"ListItem",position:2,name:product.category,item:`${SITE_URL}/category/${encodeURIComponent(product.category)}`},{"@type":"ListItem",position:3,name:product.name,item:url}]};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumb).replace(/</g,"\\u003c")}}/><ProductDetailClient product={product} allProducts={allProducts}/></>;}
