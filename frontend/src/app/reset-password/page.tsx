"use client";import {Suspense,useState} from "react";import Link from "next/link";import {useSearchParams} from "next/navigation";import {API_BASE_URL} from "@/lib/api";
function Reset(){const params=useSearchParams(),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setMessage("Les mots de passe ne correspondent pas.");return}const response=await fetch(`${API_BASE_URL}/auth/reset-password`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:params.get("token"),password})});setMessage((await response.json()).message)}return <main className="grid min-h-[70vh] place-items-center bg-[#f6f7f9] p-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border bg-white p-7"><h1 className="text-2xl font-black">Nouveau mot de passe</h1><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 h-11 w-full rounded-lg border px-3" placeholder="Nouveau mot de passe"/><input type="password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-3 h-11 w-full rounded-lg border px-3" placeholder="Confirmer"/><button className="mt-5 w-full rounded-lg bg-[var(--primary)] py-3 font-black text-white">Modifier</button>{message&&<p className="mt-4 text-sm">{message}</p>}<Link href="/login" className="mt-4 block text-center text-xs font-bold text-[var(--primary)]">Retour à la connexion</Link></form></main>};export default function ResetPasswordPage(){return <Suspense><Reset/></Suspense>}
