import React from'react';import{FileCheck2,LoaderCircle,UploadCloud}from'lucide-react';import{api}from'./lib/api';import'./secure-document.css';export default function SecureDocumentUpload({type,label,onUploaded}){const[file,setFile]=React.useState(null),[busy,setBusy]=React.useState(false),[error,setError]=React.useState('');async function upload(e){const selected=e.target.files[0];if(!selected)return;setBusy(true);setError('');try{const body=new FormData();body.append('file',selected);body.append('type',type);const result=await api('/seller/documents',{method:'POST',body});setFile(result);onUploaded?.(result)}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="secure-document"><span>{label}</span><label className={file?'uploaded':''}>{busy?<LoaderCircle/>:file?<FileCheck2/>:<UploadCloud/>}<span><b>{busy?'Envoi sécurisé…':file?file.fileName:'Choisir un document'}</b><small>PDF, JPG ou PNG · 10 Mo maximum · accès privé</small></span><input type="file" accept="application/pdf,image/jpeg,image/png" onChange={upload}/></label>{error&&<small className="document-error">{error}</small>}</div>}
