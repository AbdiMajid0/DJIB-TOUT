spring.datasource.url=${TEST_DATABASE_URL:jdbc:postgresql://localhost:55432/djibtout_test}
spring.datasource.username=${TEST_DATABASE_USERNAME:djibtout_test}
spring.datasource.password=${TEST_DATABASE_PASSWORD:djibtout_test}
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.jpa.open-in-view=false
spring.task.scheduling.enabled=false
app.mail.enabled=false
media.storage=local
jwt.secret=test-only-secret-that-is-long-enough-for-hmac-signing-2026
logging.level.org.hibernate.SQL=warn
